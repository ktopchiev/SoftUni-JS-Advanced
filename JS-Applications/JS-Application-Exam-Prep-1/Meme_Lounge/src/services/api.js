const baseUrl = 'http://localhost:3030';
const loginUrl = `${baseUrl}/users/login`;
const logOutUrl = `${baseUrl}/users/logout`;
const registerUrl = `${baseUrl}/users/register`;

const memesUrl = `${baseUrl}/data/memes`;

export default {
    loginUrl,
    logOutUrl,
    registerUrl,
    memesUrl,
}